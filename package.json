{
  "name": "title-for-project",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "vite build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^8.1.0"
  },
  "dependencies": {
    "firebase": "^12.15.0",
    "gh-pages": "^6.3.0"
  }
}
